pipeline {
    agent {
        docker {
            image 'node:16-alpine'
            //args '-u root:root' // Optional: If you need to run as root
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
        stage('Test') {
            steps {
                sh 'npm test'
            }
        }
    }
}
